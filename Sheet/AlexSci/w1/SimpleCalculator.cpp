#include "bits/stdc++.h"

using namespace std;

int main(int argc, char *argv[]) {
  long long x, y;
  cin >> x >> y;
  cout << x << " + " << y << " = " << x + y << "\n";
  cout << x << " * " << y << " = " << x * y << "\n";
  cout << x << " - " << y << " = " << x - y << "\n";

  return 0;
}
