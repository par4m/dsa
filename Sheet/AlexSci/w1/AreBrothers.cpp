#include "bits/stdc++.h"

using namespace std;

int main() {
  string a, b, c, d;
  cin >> a >> b >> c >> d;
  if (b == d) {
    cout << "ARE Brothers";
  } else {
    cout << "NOT";
  }

  return 0;
}
